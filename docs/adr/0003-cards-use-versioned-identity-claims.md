# Cards use versioned identity claims

Cards keep generated immutable IDs while unique, versioned Identity Claims
assert the grammar construction or vocabulary sense they represent. This keeps
editable teaching content out of identity and lets a later dictionary authority
attach a stronger claim without replacing learner history.
